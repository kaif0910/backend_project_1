<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 text-white p-4">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-1 text-sm" href="/logout">Logout</a>
        </div>
        <h5>you can edit your Post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea class="p-3 w-1/3 outline block resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%= post.content %></textarea>
            <input class="bg-yellow-600 rounded-md border-2 px-3 py-2 block mt-2 text-sm" type="submit" value="Save">
            
        </form>
        <form action="/delete/<%= post._id %>" method="post">
            <input class="bg-red-600 rounded-md border-2 px-3 py-2 block mt-2 text-sm" type="submit" value="delete">
        </form>
    </div>
</body>
</html>