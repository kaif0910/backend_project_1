<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 text-white p-4">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-1 text-sm" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl mb-5">Hello, <%= user.name %></h3>
        <h5>you can create a Post</h5>
        <form action="/createpost" method="post">
            <textarea placeholder="whats on your mind?" class="p-3 w-1/3 outline block resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
            <input class="bg-blue-600 rounded-md border-2 px-3 py-2 block mt-2 text-sm" type="submit" value="Create Post">
        </form>
        <% user.posts.reverse().forEach(function(post){ %>
            <div>
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <div class="post w-1/3 p-4 border-2 border-zinc-700 rounded-md ">
                    <h4 class="text-blue-500">@<%= user.username%></h4>
                    <p class="text-white tracking-tighter"><%= post.content %></p>
                    <small inline-block mt-2><%= post.likes.length %></small>
                    <a class="text-blue-500" href="/like/<%= post._id %>">
                        <%= post.likes.indexOf(user._id)=== -1 ? "like" : "unlike"; %> 
                    </a>
                    <a class="text-blue-600" href="/edit/<%= post._id %>">edit</a>
                </div>
            </div>
        </div>
        <% }) %>
    </div>
</body>
</html>